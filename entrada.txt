USE prueba1;

CREATE TABLE tbusuario (
    idusuario integer NOT NULL PRIMARY KEY,
	nombre varchar(50),
	apellido varchar(50),
	usuario varchar(15)  constraint unicos UNIQUE NOT NULL,
	password varchar(15) NULL,
	fechacreacion date constraint cehasd CHECK (fechacreacion < 10)
);