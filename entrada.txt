CREATE DATABASE compiladores2;

USE compiladores2;

CREATE TABLE usuario(
    id_usuario INTEGER,
    usuario VARCHAR(50),
    unique(id_usuario),
    constraint llaveforanea FOREING KEY (usuario) REFERENCES ESTUDIANTE(carnet)
);
